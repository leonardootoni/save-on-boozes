/**
 * Application Level constants file
 */

export enum ENV {
  PRODUCTION = 'production',
  DEVELOPMENT = 'development',
  TEST = 'test',
}

export enum Provider {
  BSW = 'BSW',
  ZYN = 'ZYN',
  CROWN_CELLAR = 'CROWN_CELLAR',
}

export enum ParserURL {
  BSW = 'https://www.bswliquor.com',
}
